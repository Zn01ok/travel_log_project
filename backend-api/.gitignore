# Byte-code files (Python 字节码)
*.pyc
__pycache__/             # Python 字节码缓存目录
.pytest_cache/           # pytest 测试缓存
.mypy_cache/             # MyPy 类型检查缓存

# IDEs (后端 IDE 文件)
.idea/
.vscode/
*.iml
*.ipr
*.iws

# Virtual environment (虚拟环境，非常重要)
venv/                    # 常见的虚拟环境目录名
env/                     # 另一个常见的虚拟环境目录名
.env/                    # Docker Compose 等可能生成的 .env 文件夹

# Dependencies (通过 pip 安装的依赖)
*.egg-info/
build/                   # Python 包的构建目录
dist/                    # Python 分发包目录

# Logs
*.log
logs/

# Database files (本地开发数据库文件)
*.sqlite3
*.db
*.db-journal

# Environment variables (本地环境变量，如果不是在根目录 .gitignore 处理)
.env
.flaskenv
!.env.example

# Uploaded files (如果文件直接存储在后端项目目录下，例如用户上传的图片)
/uploads/
/media/

# Test reports
.coverage                # coverage.py 报告文件
htmlcov/                 # coverage.py HTML 报告目录

# Other temporary files
*.py[cod]                # 各种 Python 字节码变体
*$py.class
